<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bdqn.oaproject.dao.CheckDao">
    <insert id="add" parameterType="Check">
        insert into `check`(auditId,resultId,copinion,cdate) values
        (#{auditId},#{resultId},#{copinion},#{cdate})
    </insert>
</mapper>